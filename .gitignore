# Ignore files created by the build process.
Makefile
parrot/
parrot_install/
# Ignore any -file- named parrotlog, but not a directory.
parrotlog
!parrotlog/
*.o
# This exclusion should perhaps be modified if we add C code.
*.c
*.pbc

# Ignore vim swap files.
.*.swp
